const data = ['D1', 'D2', 'D3'];

data.filter(function (value, index) {
    console.log(`${index}: ${value}`);
});

// Side Notes:
//  We can use method chaining on JS.
//  ex. str.toLowerCase().trim()

