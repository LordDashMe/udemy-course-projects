// Lecture:
//   Number implementation.
//   The Order of Operations: PEMDAS.

let baseNumber = 5.5;
let addition = baseNumber + 2; // 7.5
let substraction = baseNumber - -1; // 6.5
let multiplication = baseNumber * 2; // 11
let division = baseNumber / 2; // 2.75 

console.log(addition, substraction, multiplication, division);

let pemdas = (5 + 1) / 2; // 3

console.log(pemdas);

// Challenge

let studentScore = 18;
let maxScore = 20;
let percentage = (studentScore / maxScore) * 100; // 90%

console.log(percentage + '%');
