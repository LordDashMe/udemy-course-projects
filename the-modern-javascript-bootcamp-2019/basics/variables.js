// Lecture:
//  Variables definition.
//  Assigning new value to the variable.

// Rules:
//  1. No duplicate variable names. let num; let num;
//  2. Variable must not starting with numbers. ex. let 1_num = 1;
//  3. Reserved keyword can't be set as variable name. ex. let let = 2;

let petName = 'Dash';

petName = 'Spot';

console.log(petName);
